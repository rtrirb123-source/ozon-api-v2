# Codex 协作与生产安全规则

本文件适用于本仓库及其全部子目录。任何电脑、Codex 对话或自动化任务在修改本项目之前，都必须先遵守本文件。

## 开始工作前

1. 先读取本文件，再检查当前分支、`git status`、`HEAD` 和 `origin/main`。
2. 如有未提交修改，先保存并提交到当前任务分支；禁止直接覆盖、丢弃或混入其他任务。
3. 开始新任务前先获取最新 `origin/main`，并从最新主线创建独立功能分支。
4. 历史对话、旧电脑、旧备份和生产快照只能用于核对或恢复，禁止整包覆盖当前代码。
5. 已确认的库存看板历史基准为 `8a62f2b97265836a02831d36f5e5a487945ed9fc`；它只作为恢复参照，日常开发始终以最新 `origin/main` 为准。

## 多电脑与分支协作

1. 每台电脑、每个并行任务使用独立分支，禁止在同一分支同时开发。
2. 服务器 `/opt/ozon-api-v2` 若正处于其他任务分支，不得切换、重置或覆盖；使用独立 worktree 或在其他目录工作。
3. 合并前必须说明分支名、提交号、修改文件、测试结果及冲突处理情况。
4. 未经用户明确确认，不得直接向 `main` 推送。
5. 禁止 `git push --force`、`git reset --hard`，禁止用旧文件覆盖生产文件。
6. 发现未知修改时立即停止写入，先确认修改来源并保护现场。

## 生产部署

1. 部署前确认目标提交、工作区状态，并生成可恢复快照。
2. 只部署已确认提交；不得把整个旧目录直接复制到生产环境。
3. 部署后必须核验：Git 提交号、工作区状态、服务状态、页面 HTTP 状态、关键 API 和定时任务。
4. 数据库、凭证、NAS 登录信息、运行数据、备份及 `.env` 文件不得提交到 Git。
5. 未经用户明确要求，不得改变定时任务时间、库存公式、表格结构或隐藏栏。

## 库存看板受保护业务规则

1. 商品总库存 = FBO库存 + FBO在途 + FBW + FBS手填库存总数 + 未分配库存。
2. 缺货量以“商品总库存”为计算口径。
3. 当前没有可靠 FBW 数据源：历史 FBW 遗留值保持清零，FBW 固定显示 0；只有未来接入经确认的可靠接口后才参与计算。
4. 同一 WB 卡片关联多个 SKU 时，不得重复计算 FBW；未来恢复 FBW 数据时必须按唯一归属去重。
5. 未分配库存单元格显示总件数；鼠标悬停显示 SKU、箱唛、箱号和单箱数量，浮框不得遮挡当前输入框。
6. 未分配库存按库存看板商品的 SKU 条码与 NAS 表格 SKU 匹配；表格箱数乘以单箱数量得到未分配件数。
7. 每日发货数同步必须持久化保存数据日期、最近成功时间和失败原因；手填数据不得被错误的 0 覆盖。
8. 库存自动更新时间为北京时间每天 08:00、15:00；若具体同步任务采用错峰分钟，必须保持页面标注和实际任务一致。
9. 世晟隐藏栏及其他已确认结构不得擅自删除或重排。
10. 修改 WB、Ozon 或其他独立看板时，不得顺带重写库存看板文件或回退库存业务逻辑。

## 修改完成后的最低验证

1. 执行与改动相关的语法检查、接口检查和页面检查。
2. 对库存逻辑至少复核商品总库存、缺货量、FBW固定为0、未分配库存详情和每日发货数状态。
3. 提交前再次检查 `git diff`，确认没有凭证、运行数据、备份或其他任务文件。
4. 本文件的保护规则不得在未获用户明确同意时删除或放宽。
